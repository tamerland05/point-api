[project]
name = "point-shared"
version = "0.1.0"
description = "Point models shared repo"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
    "pytoniq-core>=0.1.46",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"